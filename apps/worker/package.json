{
  "name": "@overhead/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "cd ../.. && bun apps/worker/dist/index.js",
    "build": "bun build src/index.ts --target bun --outdir dist --sourcemap=linked"
  },
  "dependencies": {
    "@overhead/core": "workspace:*",
    "@overhead/database": "workspace:*",
    "@overhead/flight-tracking": "workspace:*"
  }
}
